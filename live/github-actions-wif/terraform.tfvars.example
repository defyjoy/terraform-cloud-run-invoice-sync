# Reference only — real values live in .auto.tfvars, not this file. Every variable is required
# (no defaults) — copy this file's shape when standing up a second instance of this stack.

project_id = "yeti-504903"

github_owner = "defyjoy"
github_repo  = "terraform-cloud-run-invoice-sync"
github_ref   = "refs/heads/main"

state_bucket = "yeti-terraform-state-bucket"

deploy_account_id = "github-deployer"
